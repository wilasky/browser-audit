PORT=3000
HOST=0.0.0.0
# Comma-separated list of valid Pro API keys
API_KEYS=change-me-key-1,change-me-key-2
# Rate limit: max requests per minute per IP
RATE_LIMIT_MAX=60
# SQLite file path
DB_PATH=./data/browseraudit.db
# Log level: trace | debug | info | warn | error
LOG_LEVEL=info
